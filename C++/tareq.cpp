#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    cin >> s;
    string ary[s.length()];
    for (int i = 0; i < s.length(); i++)
    {
        string store;
        if(s[i]==' ' && i<s.length()){
            ary->append(store);
        }
    }
    

    return 0;
    
}